###基本数据结构——栈

场景：浏览器的前进、后退功能

栈的主要特点：后进者先出，先进者后出(一种受限制的线性表，只允许在一端插入和删除数据)

栈的操作：

入栈和出栈：在栈顶插入一个数据和在栈顶弹出一个数据;

1、基于数组的顺序栈，栈的大小是固定的；
2、基于链表的链式栈，大小可以动态增加；

- 栈的一些使用场景

函数调用中的应用：栈主要是用来保存函数使用的临时变量

表达式求值中的应用

栈在括号匹配中的应用


思考：

1、栈的应用，讲到函数调用栈来保存临时变量，为什么函数调用要用栈来保存临时变量呢？用其他数据结构不行吗？

2、JVM内存管理中有一"堆栈"的概念。栈内存用来存储局部变量和方法调用，堆内存用来存储java中的对象，那么JVM里面说的
栈和我们这里所说的栈是不是一回事？如果不是，那它为什么又叫做栈？


        









